enum EDevices {
  MOBILE = "mobile",
  TABLET = "tablet",
  DESKTOP = "desktop",
}

enum newEDevices {
  DESKTOP = "lg",
  TABLET = "md",
  MOBILE = "xs",
}

enum ESizes {
  "mobile" = "xs",
  "tablet" = "md",
  "desktop" = "lg",
}

export { EDevices, ESizes, newEDevices };
