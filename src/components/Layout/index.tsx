import { Container } from "semantic-ui-react";
import { MainContent } from "../";

type Props = {};

export const Layout = (props: Props) => {
  return (
    <Container>
      <MainContent />
    </Container>
  );
};
